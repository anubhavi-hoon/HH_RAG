# Backend base URL. src/api/rag.ts is the only module that reads it;
# no other frontend file may reference the backend host.
# Copy to web/.env to override the http://localhost:8000 development fallback.
VITE_API_BASE_URL=http://localhost:8000
